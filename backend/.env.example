# FlowMind Analytics - Environment Variables Template
# Copy to .env and set actual values in secure vault

# Unusual Whales API
UW_API_TOKEN="<set in vault>"

# TradeStation OAuth (populated dynamically via auth flow)
TS_TOKEN="<oauth token - populated dynamically>"
TRADESTATION_API_KEY="<set in vault>"
TRADESTATION_API_SECRET="<set in vault>"
TS_CLIENT_SECRET="<set in vault>"

# TradeStation OAuth Configuration
TS_BASE_URL="https://api.tradestation.com/v3"
TS_TOKEN_URL="https://signin.tradestation.com/oauth/token"
TS_REDIRECT_URI="http://localhost:8080"
TS_TOKEN_MARGIN_SEC=120

# Database
MONGO_URL="mongodb://localhost:27017/flowmind"

# Application
ENV="development"
DEBUG="true"