# Production .env for frontend
# This file is NOT used in production - kept for reference only
# In production, REACT_APP_BACKEND_URL is set to empty string
# so the frontend uses relative URLs (same domain via Caddy)

# Local development uses codespace URL:
# REACT_APP_BACKEND_URL=https://sturdy-system-wvrqjjp49wg29qxx-8000.app.github.dev

# Production uses relative URLs (empty value):
REACT_APP_BACKEND_URL=
